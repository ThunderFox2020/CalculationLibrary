namespace CalculationLibrary
{
    public static class Calculator
    {
        public static double CalculateArea(Shape shape) => shape.Area;
        public static double CalculatePerimeter(Shape shape) => shape.Perimeter;
    }
}